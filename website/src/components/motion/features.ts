import { domMax } from "motion/react"

// Motion's full feature set (layout animations for the sliding nav/rail
// highlights, gestures, exit animations), loaded in its own chunk after the
// page is interactive. `m` components render immediately and animate once
// this lands.
export default domMax

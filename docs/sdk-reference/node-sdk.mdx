---
title: "Node.js SDK"
description: "Placeholder."
owner: "engineering"
type: "reference"
---

## Installation

How to add the Node.js SDK (`iii-sdk`) to a project.

## Initialization

Connecting to the engine and obtaining the SDK handle. Always call the variable `worker` and not
`iii` so when we do `worker.registerFunction()` it makes more contextual sense.

## Connection lifecycle

How the SDK connects to the engine, registers functions, heartbeats, and reconnects with
re-registration on disconnect (exponential backoff with jitter, configured via
`IIIReconnectionConfig`).

## Methods

The functions exposed by the Node SDK for registering, invoking, and tearing down workers.

### registerWorker

Register this worker with an iii engine.

#### InitOptions

Options accepted when constructing the worker instance (worker name, timeouts, `reconnectionConfig`,
`otel`, metrics reporting).

### registerFunction

Register a callable function on this worker, with optional request and response JSON Schema formats.

### registerTrigger

Bind a registered function to a configured trigger instance.

### trigger

Invoke a registered function or fire a trigger from Node.

### shutdown

Disconnect from the engine and release resources, flushing any pending telemetry.

### registerTriggerType

Declare a new trigger type that this worker can source events from. **Consider removing**

### unregisterTriggerType

Remove a previously registered trigger type. **Consider removing**

<Note>
  Logging and telemetry surfaces (logger methods, OpenTelemetry init, custom spans, worker metrics, log emission/subscription) are documented with the [iii-observability worker](#) — they live there because that worker owns logging and telemetry end-to-end.

Channel surfaces (`Channel`, `ChannelReader`, `ChannelWriter`, `ChannelDirection`,
`StreamChannelRef`, `createChannel`) are documented with the [iii-worker-manager](#) — channels are
managed by that worker end-to-end.

</Note>

## Subpath exports

Additional modules importable from sub-paths (e.g., `iii-sdk/telemetry`).

## Types

**Types should be visually separated from the functions in the docs**

### MessageType

Wire message-type discriminator used by the SDK.

### FunctionRef

Typed reference to a registered function.

### IIIReconnectionConfig

Engine-WebSocket reconnection behavior — `maxRetries`, `initialDelayMs`, `backoffMultiplier`,
`maxDelayMs`, `jitterFactor`.

### HttpAuthConfig

Auth configuration accepted by HTTP-invoked function registrations.

### HttpInvocationConfig

Config used when registering an external HTTP endpoint as an iii function.

### RegisterFunctionFormat

JSON Schema shape used for `request_format` and `response_format` on registered functions.

### RegisterFunctionMessage

Wire shape sent when registering a function.

### RegisterFunctionOptions

Options passed alongside a function registration.

### RegisterServiceInput

Input shape for `registerService`.

### RegisterTriggerInput

Input shape for `registerTrigger`.

### RegisterTriggerMessage

Wire shape sent when binding a trigger.

### RegisterTriggerTypeInput

Input shape for `registerTriggerType`.

### RegisterTriggerTypeMessage

Wire shape sent when declaring a trigger type.

### RemoteFunctionHandler

Handler signature for functions invoked remotely.

### Trigger

Configured trigger instance.

### TriggerHandler

Handler signature for trigger-driven invocations.

### TriggerRequest

Request payload delivered to a trigger handler.

### TriggerTypeRef

Typed reference to a registered trigger type.

### IStream

Stream consumer interface.

### DeleteResult

Result of a delete operation.

### StreamSetResult

Result of a stream set operation.

### StreamUpdateResult

Result of a stream update operation.

{/* generated by iii-skill-render. DO NOT EDIT (changes here are overwritten on the next render). Edit tutorials/incremental-adoption/overview.mdx. */}

# Overview


This tutorial brings iii into an existing system step by step. First you'll wrap an existing HTTP
service as an iii function so the rest of the system can address it. Then you'll move slow work
behind an iii queue so callers return immediately and retries are handled by the queue. Finally
you'll migrate persistence into iii's state primitives. By the end your system runs on iii without a
big-bang rewrite.

## Wrap an existing API

Front an existing HTTP service with an iii function so it becomes triggerable from the rest of the
system.

## Offload work to a queue

Move long-running work behind an iii queue so callers return immediately and retries are handled by
the queue.

## Migrate persistence

Gradually move state out of your existing store into iii's state primitives without a big-bang
rewrite.
